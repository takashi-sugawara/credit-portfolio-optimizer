# 2026-07-20 開発ログ

## 1. リポジトリ整備
- 未追跡だった3ファイル(`*.code-workspace`, `docs/test_report.md`,
  `docs/walkthrough.md`)を `.gitignore` に追加

## 2. Phase 2以降のデプロイ方針を決定
- Streamlit Community Cloudは単一のStreamlitアプリしか動かせず、別プロセスの
  FastAPI・Celery/Redisワーカー・Streamlit以外のフロントエンドを配置できない
  という技術的制約を確認
- 方針: ブランチは分岐せず `main` 一本のまま、ディレクトリで分離する
  （`app.py` + `domain/` はStreamlit Cloud向け、新設する `api/` はAzure向け）。
  既存のStreamlitデモは廃止せず維持する
- `docs/ROADMAP.md` の Phase 2 に "Deployment Strategy" 節として記録

## 3. Phase 1残タスクの実装
判断軸として「config化することで、変更の承認プロセス・関与者が実際に変わるか」
を採用（現場が触りたいかどうかだけでは判断しない）。

- **Pyomoモデル分離**: `solve_optimization` / `get_ipopt_path` /
  `get_efficient_frontiers` / `calculate_portfolio_profit` /
  `run_sensitivity_analysis` を `domain/solver.py` に切り出し。
  UI依存（`st.cache_data`、翻訳ラベル）を除去し、`app.py` は薄いラッパーに
- **閾値の外部化**: `config/business_rules.yaml` を新設し、与信枠メニュー・
  主婦学生上限・シナリオ倍率（Optimistic/Neutral/Pessimistic）を集約。
  `domain/rules.py` と `app.py` の両方がここを単一ソースとして参照するように変更
- ローカルでStreamlitアプリを実際に起動し、フロンティア表示・最適化実行・
  感度分析Tornadoチャートまで一通り動作確認
- コミット: `239ee98`

## 4. GitHub Actionsの調査
- pushしたワークフローが `Queued` のまま進まない事象を発見
- Actions使用量CSVで「当日分のコンピュート時間の計上が一切ない」ことを確認し、
  UI表示の古さではなく実際に未実行と判断
- githubstatus.com で **GitHub Actions側の障害** を特定。リポジトリ側の問題
  ではないと結論
  - インシデント名: "Incident with GitHub Actions"
  - 発生: 2026-07-19 23:34 UTC（最初の investigating 更新）
  - 影響コンポーネント: Actions（partial outage）, Pages（degraded）,
    API Requests（partial outage）, Issues（degraded）
  - 本記録時点（2026-07-20 01:11 UTC 更新分）でもまだ `investigating`
    ステータスで未解決
  - パーマリンク: https://www.githubstatus.com/incidents/8vfyvq16hzh9
    （shortlink: https://stspg.io/w8d77c7t94zf）
  - この障害により、当日pushしたコミット（`239ee98` 等）のCI/Azureデプロイ
    ワークフローが `Queued` のまま実行されていない。次週末に確認するまで
    放置する場合、Azure側に不具合が見つかっても「コード起因ではなく、この
    障害でデプロイが未反映だった可能性」をまず疑うこと

## 5. Issue管理
- GitHub Projects上の issue #13（Pyomoモデル分離）・#14（閾値外部化）の内容を
  確認し、完了条件を満たしていることを確認 → Doneに更新

## 未完了・持ち越し
- GitHub Actions障害の影響で、CI（Unit Tests）とAzureへのデプロイ
  （commit `239ee98` および `b2769b5`）が `Queued` のまま実行待ち。
  障害復旧後にキューが流れるはずだが、要確認
- Phase 2（2a-pre: Dockerfile整備）は未着手
